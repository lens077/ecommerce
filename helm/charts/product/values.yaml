replicaCount: 1
enabled: true
image:
  repository: "ccr.ccs.tencentyun.com/sumery/product"
  tag: pre
  pullPolicy: Always
service:
  type: LoadBalancer
  ports:
    http:
      enabled: true
      port: 30003
      targetPort: 30003
    grpc:
      enabled: false
      port: 30003
      targetPort: 30003
env:
  - name: SERVICE_NAME
    value: "product-service"
  - name: CONSUL_ENABLED
    value: "true"
  - name: CONSUL_ADDR
    value: consul-expose-servers.consul.svc:8500
  - name: CONSUL_PATH
    value: "ecommerce/product/pre.yml"
  - name: CONSUL_SCHEME
    value: "http"
  - name: CONSUL_INSECURE_SKIP_VERIFY
    value: "true"
secret:
  enabled: true
  name: "product-db-secret"
  data:
    DB_SOURCE: "postgresql://postgres:password@host:5432/db?sslmode=require"
dbCaCert:
  enabled: true
  secretName: "pg-ca-cert"
  mountPath: "/etc/ssl/certs"
  fileName: "pg_ca.crt"
resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "1"
    memory: "256Mi"
