enabled: true

replicaCount: 1

image:
  repository: "ccr.ccs.tencentyun.com/sumery/connect-react-example"
  tag: "dev"
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 3000
  targetPort: 80
  nodePort: 31800

autoscaling:
  enabled: false

# helm install connect-example .
