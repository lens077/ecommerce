---
{
  # 占位默认值;实际值来自 umbrella helm/values.yaml 的 frontend: 段。
  enabled: true,
  replicaCount: 1,
  image: {
    repository: "",
    tag: "",
  },
  containerPort: 80,
  servicePort: 30080,
  gatewayURL: "",
  # Disabled until Helm, bare manifests, VPA target and rollback procedure
  # migrate together. Enabling this alone would break deploy parity.
  rollouts: {
    enabled: false,
    autoPromotionEnabled: false,
    vpaEnabled: true,
  },
}
