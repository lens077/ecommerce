# 供应链扫描基线

这里保存 zizmor 与 Trivy 的存量告警指纹，不保存秘密或完整扫描报告。

规则：

- 新告警必须修复，不能为了让 CI 变绿而扩基线。
- 修复存量告警后，应删除对应指纹，使基线只减不增。
- Gitleaks 不使用基线；它只扫描 PR 提交范围，任何新增秘密直接阻断。
- 指纹生成逻辑位于 `scripts/supply-chain-pr.sh`。工具升级导致结果模型变化时，应人工复核差异，不能静默重建基线。
