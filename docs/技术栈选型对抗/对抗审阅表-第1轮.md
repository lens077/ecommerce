# 对抗审阅表 — 第 1 轮（2026-08-20）

> 由 AgentTeams `tech-radar-adversarial` 的 captain（claude-fable-5，即《claude-选型结论》作者）编制。
> 参战三稿：**主稿**=`claude-选型结论-2026-08-20.md` · **codex 稿**=`codex的选型.md`（openai/gpt-5.6-terra）· **claude2 稿**=`claude2-日志栈拍板-VictoriaLogs+Vector.md`（anthropic/claude-fable-5）。
> captain 同时是主稿作者，存在立场冲突；处理方式：captain 只负责事实核验与出题，主稿同样接受两成员攻击，captain 的自我改判在 §B 先行公开。

## A. 共同事实基础（captain 实测核验，2026-08-20，对抗不得再争论这些事实本身）

| # | 事实 | 来源 | 对三稿的影响 |
|---|---|---|---|
| A1 | 集群仅 2 节点（node1/node2），**全 arm64**，K8s v1.36.3，默认 SC `openebs-lvm` | `kubectl get nodes/sc` 实测 | codex 稿 ✅ 报对；主稿按旧基线（多节点）写；任何「3 节点 raft/R3 副本」方案当前无法物理成立 |
| A2 | **CNPG 已在集群内**：`postgresql/pg-main` 单实例 healthy；**Pigsty(192.168.3.210) 已关机**，10 服务 pre.yml 已切 `pg-main-rw.postgresql.svc` | `kubectl get clusters.postgresql.cnpg.io` 实测 + `TODO.md`「2026-08-19 集群重建后 GitOps 重新接线」行 | **主稿 §3「保持 Pigsty」前提失效**；codex/claude2 与现实一致 |
| A3 | **ES 已退役**（address/search 两服务因此 CrashLoop）；**Meilisearch v1.53 已装**（`search/meilisearch:7700`）且 **2026-08-16 已拍板 ES→Meilisearch**（约 2 人日迁移计划在册） | `TODO.md` 搜索引擎切换段 + 集群重建行 | **主稿 §2 推 Typesense 与既定拍板冲突**；codex（既成事实）/claude2（主推 Meili）与拍板一致 |
| A4 | 服务缓存已指向 `redis.redis.svc:6379`（后端 6380 TLS）；dragonfly 部署仍在但 pre.yml 已切走 | `TODO.md` 集群重建行 + `kubectl` 可见 dragonfly pod 仍 Running | codex「缓存实为 redis」基本属实（补充：dragonfly 是残留部署待清理）；主稿/claude2 基线写 Dragonfly 已过期 |
| A5 | Strimzi/Kafka operator 仍部署（kafka ns）但应用侧零消费、数据面不作可用前提 | `kubectl` + `TODO.md` | 三稿一致：退役对象 |
| A6 | **claude2 文件物理截断**：全文仅 6,594 字节，正文断在 §3 表格第二行，末行含字面 `(+13332 more chars)` 残迹；§3 后半至 §12 论证缺失，仅「结论速览」表可比 | `wc -c` + 读文件实测 | claude2 需在本轮确认速览表立场并重建关键论据 |
| A7 | MinIO 上游仓库已归档（2026-02/04） | 三稿一致 + GitHub API 实测 | 无争议 |
| A8 | 集群 pod 大量 Completed/重启痕迹（节点多次重启史）；claude2 稿自述「OOMKill 前科」 | `kubectl get po -A` 观感 + claude2 稿 | 资源脚印论据的现实权重上调 |

## B. captain（主稿）先行自我改判

| 项 | 原立场 | 改判 | 理由 |
|---|---|---|---|
| §3 PG | 保持 Pigsty，CNPG 不上 | **CNPG 既成事实，改为「补第二实例副本 + Barman Cloud Plugin PITR + 异地备份 + 恢复演练」** | A2：前提失效。原稿「pgBackRest 异地化」待办转译为 CNPG 备份异地化 |
| §2 门面 | Typesense 3 节点 raft 主选 | **部分改判：维持 Meilisearch 拍板合理**；HA 关注降级为「备份 + 快速重建 + 索引可全量重放」验收项 | A1+A3：2 节点组不成 3 节点奇数仲裁，Typesense 的 OSS-HA 优势无法兑现；且 2026-08-16 已有拍板与部署既成事实 |
| §2 备注 | — | 保留一条技术警示进雷达：Meilisearch OSS 单机形态、HA 属 Cloud（v1.19 起 EE=BUSL），扩到 3 节点那天需重议 | 事实成立，仅时点不成立 |

## C. 逐项 diff（✅=实质一致，★=活分歧，◇=因 A2/A3 已由事实裁决）

| 层/项 | 主稿 | codex 稿 | claude2 稿 | 判定 |
|---|---|---|---|---|
| §1 broker | NATS JetStream ✅ | NATS 🧪（第三故障域/奇数仲裁前不接交易事件） | NATS ✅（P-1） | ★ **D5：上线节奏** |
| §1 relay/CDC | 自写 relay；pgstream 备选 | SQL Outbox relay；原始 CDC 用 pgstream | 自写 relay | ✅ |
| §1 Numaflow | 🟡 观察 | 未提（不上） | 🟡 触发式 | ✅ |
| §2 门面 | Typesense→已自我改判 | Meilisearch CE ✅ | Meilisearch 主推 | ◇（按 B 收敛为 Meili；遗留警示条款） |
| §2 向量 | pgvector 起步→Qdrant | **Meili 内置 hybrid/vector 先行**，之后再评估 pgvector | pgvector 起步→Qdrant | ★ **D4：向量起步位** |
| §3 PG | 已自我改判（CNPG 补强） | CNPG 既成事实 + 副本/PITR/告警 | CNPG 引进 | ◇ 收敛：CNPG 补强路线 |
| §3 OLAP | ClickHouse 单节点 | ClickHouse ▶（埋点先批量写） | ClickHouse + PeerDB | ✅（PeerDB 时点小差异） |
| §4 授权 | Casbin 保持 + OpenFGA 新域试点 | 同（双跑切入） | OpenFGA 商家多操作员开工时 | ✅ |
| §4 IdP | Casdoor 保持 | Casdoor 保持 | （截断未见） | ✅ |
| §4 凭据 | **ESO + OpenBao + SOPS 引进** | **⏸ 先改治理规则再选工具**（AGENTS.md 硬规则 4 只认 Config Center + 本地/Secret） | ESO + SOPS 列 P-0 | ★ **D1：程序 vs 紧迫性（本轮头号分歧）** |
| §5 网关/网格/LB | 保持自研 / 不上网格 / Cilium 零新增 | 同 | （截断；速览未列） | ✅ |
| §6 发现 | Consul 退役 → Service DNS | 同（灰度逐路由切） | （截断） | ✅ |
| §7 KEDA | ✅ 引进 | ▶ 但 **NATS 未落地前不抢先部署** | ✅ P-1 | ✅（次序小差异并入 D5） |
| §7 Kruise | 单服务 CloneSet 试点 | 仅 ImagePullJob | P-3 引进 | ★ D9（低权重） |
| §8 日志 | **VictoriaLogs 直接拍板**，先单机 | **Vector→Loki+VL 双写验证后再拍** | VL + Vector 列 P-0（其原始主题） | ★ **D2：切换程序** |
| §8 trace | Jaeger 保持（v2） | Jaeger v1 则先升 v2 | （截断） | ✅ |
| §8 profiling | Parca PoC（Apache 许可优先） | Pyroscope 后续接入（一句带过） | （截断） | ★ D8（低权重） |
| §9 金丝雀 | Rollouts ✅（依赖 §6 改造先行） | Rollouts 条件采用（多副本+容量余量后） | Rollouts P-2 | ✅ |
| §9 构建分发 | ko 试点 + **Spegel 试点** | ko 🧪 + **Spegel 两节点收益存疑、仅可无损试装** | ko P-2 | ★ D7（低权重） |
| §9 混沌 | 缓 | 未列（不上） | **Chaos Mesh P-2 引进** | ★ **D6** |
| §9 内环 | mirrord 替 Okteto | 未提 | （截断） | 无对抗（留第 2 轮） |
| §10 备份 | Velero 立即 | **CNPG Barman 插件管 PG 一致性 + Velero FSB 管其余**（点名「Velero 文件备份≠DB 一致性恢复」） | Velero + CNPG P-2 | ✅ codex 表述最精确，主稿吸收 |
| §10 对象存储 | SeaweedFS 迁移 | SeaweedFS ▶ | （截断） | ✅ |
| §11 | Tetragon + Kyverno + Trivy + cosign（verifyImages） | 同（Tetragon 先观察后逐 workload 阻断） | Trivy + cosign + Kyverno P-0 | ✅ |
| §12 Dapr | ❌ | ❌ | （截断） | ✅ |
| §12 开关 | **OpenFeature + Flipt v2**（FCL 已标注） | **OpenFeature + flagd**（或 GOFF 库模式）；Flipt FCL 不适合作默认 | （截断） | ★ **D3：引擎选型** |

## D. 第一轮对抗题目与分工

> 规则：对每个分配到的分歧给出 **维持 / 改判 / 部分改判** + ≤3 条论据（标注证据是否可实查）+ 若改判写清改成什么。
> 每人另交「对他稿的攻击」≥2 条/每稿：目标稿 + 条目 + 问题类型（事实错/前提过期/风险低估/许可证/运维脚印/程序违规）+ 建议改判。
> 允许只读核验（读仓库文件、`kubectl get` 只读、web 检索）；**禁止修改任何文件**；总输出 ≤1200 字，写进任务 output。

| 分歧 | codex 任务 | claude2 任务 |
|---|---|---|
| D1 凭据 | 为「⏸ 先改治理」辩护：回应「PII/凭据泄露是 P0 整改，程序性暂缓是否等于无限期挂起」 | 为「ESO+SOPS 列 P-0」辩护：回应 codex 的程序异议（AGENTS.md 硬规则 4 未授权新凭据面），给出合规引入路径 |
| D2 日志切换 | 为「双写过渡再拍」辩护：量化双写的额外成本与回退价值 | 为「P-0 直切 VL」辩护：说明为何双写在 2 节点资源现实下反而是负担；日志栈是你的原始主题，拿出最强论据 |
| D3 开关引擎 | 为 flagd/GOFF 辩护：回应「flagd 974★ + 请求路径多一跳」质疑 | 攻击或支持任一方（你稿截断未表态，本轮必须表态） |
| D4 向量起步 | 为「Meili 内置 vector 先行」辩护：给出 Meili hybrid 在 CE（MIT）版的可用边界证据 | 为「pgvector 起步」辩护：CNPG 镜像装 pgvector 的可行性（注意 A2：已不是 Pigsty，你稿此段论据需重建） |
| D5 NATS 节奏 | 为「🧪 交易事件等奇数仲裁」辩护：2 节点下 JetStream R1/R3 的具体形态建议 | 为「✅ 直接上」辩护或部分改判：非交易事件先行是否可立即开工 |
| D6 混沌 | 攻击 claude2 的「Chaos Mesh P-2」（若你认为该缓） | 为「Chaos Mesh P-2」辩护：2 节点 + 单人团队下的最小混沌形态，或自我改判 |
| D7 Spegel | 维持你的「两节点收益存疑」并给证据 | 表态（你稿截断） |
| D8 profiling | 表态 Pyroscope vs Parca（一句即可） | 表态（一句即可） |
| 攻击回合 | 攻主稿 ≥2 条 + 攻 claude2 稿 ≥2 条（A6 截断问题可用） | 攻主稿 ≥2 条 + 攻 codex 稿 ≥2 条 |
| 复核回合 | 复核 captain 的 B 节自我改判是否到位、有无漏改 | 同左 |

## E. 结果登记（captain 于 2026-08-20 回填）

> 两份完整输出存于团队任务 t1/t2 的 output（[`团队存档/team.json`](团队存档/team.json)）。本节登记裁决。

### E.1 逐分歧裁决

| 分歧 | codex 立场 | claude2 立场 | captain 裁决 |
|---|---|---|---|
| D1 凭据 | 部分改判：工具仍⏸，吊销/轮换/盘点先行为 P0，治理修订后试点 | 维持 ▶：治理修订本身是 P-0 第一子任务（≤0.5 人日，走硬规则 5 evolution-log），ESO 产出的就是受管 Secret，是规则 4 的执行工具非例外；现状（泄露口令无轮换）本身是违规态 | **合流为次序化方案**：①立即用现有手段吊销/轮换/盘点（止血不等工具，采 codex）②同步提交 AGENTS.md 硬规则 4 治理修订（采 claude2：修订是子任务不是暂缓理由）③修订合入即开工 ESO+OpenBao 试点 ④新链路完成正式轮换闭环后 P0 关闭。净结论：⏸→**▶（带前置子任务）** |
| D2 日志切换 | 维持双写：实测 11.29 MiB/h，额外约 271 MiB/d，可验脱敏/看板/丢重，失败只关 VL sink | 维持直切但主动让步：Loki 耦合面实测仅 6 处 datasource 引用 + 0 条告警挂 Loki；无界双写的对比查询正是 Loki OOM 复现机理（TODO L159）；VL 兼容 Loki push API 分钟级回退；**接受 ≤72h 时间盒冒烟双写** | **有界双写后切主**（codex 的纪律 + claude2 的边界）：双写时间盒 ≤72h，验收清单=PII 脱敏反例测试、3 个 logs 面板改写、查询等价抽查、丢/重检查；过则立即切主，Loki 冻结只读至保留期满退役 |
| D3 开关引擎 | 维持 flagd/GOFF，Flipt FCL 不能默认接受 | 首选自研 Config Center provider（零新组件，复用 pg_notify/审计/回滚），次选 GOFF 库模式；反 Flipt 与 flagd | **主稿改判**：OpenFeature ✅ 不变；引擎首选 **Config Center provider**、次选 **GOFF 库模式（MIT 进程内）**；Flipt v2 降为「需要运营 UI/实验分析」的条件项；flagd 不引。攻击成立点：主稿对 Redpanda BSL 一票否决却接受 FCL 当默认，许可证口径自相矛盾 |
| D4 向量起步 | 维持 Meili CE hybrid 先行；质疑 CNPG 未证明含 pgvector | 论据重建：CNPG 官方 standard 操作数镜像**内置 pgvector**（postgres-containers），换 imageName+CREATE EXTENSION 即可；提出合成方案=PG 存权威 embedding，Meili userProvided 作召回层 | **组合裁决**：pgvector（CNPG standard 镜像）为 embedding 权威存储，Meili hybrid（userProvided）作召回展示层，Qdrant 仍是规模化触发项。落地前实证一条：CNPG standard 镜像 pgvector 版本与 CREATE EXTENSION 可用性 |
| D5 NATS 节奏 | 维持 🧪：R1=1 pod+1 PVC 只接非交易可重建消费；R3 须第三故障域+反亲和+演练后才接交易 | 维持 ✅+让步：outbox 是真相源，R1 丢 broker=重放不丢数据（正确性不依赖 R3）；search 索引喂养链路等米下锅；交易域接入前补「R1 故障→积压重放」演练 | **收敛**：NATS R1 立即上（先接 search 索引/埋点等可重建流）；交易事件接入前置=R1 故障重放演练通过（不必等第三节点，采 claude2 的 outbox 论据 + codex 的形态细节）；R3 绑定第三故障域。附带推翻 codex「KEDA 等 NATS」：cron/prometheus scaler 先行即有大促预热价值（claude2 攻击成立，codex 稿自引证据） |
| D6 混沌 | 维持缓，降为恢复演练后的触发项 | **自我改判**：P-2 固定项→触发式最小形态（徒手 staging 演练先行；需网络级注入再上，无 dashboard、仅 Pod/NetworkChaos、限 staging） | **收敛为触发式**：先徒手演练 Runbook；触发条件=需要 NATS 分区/PG failover 级注入 |
| D7 Spegel | 维持存疑：两节点至多一 peer，首次冷拉仍打仓库 | 观察（同 codex）；并攻主稿「✅试点」前提过期、与 zot 否决逻辑自相矛盾 | **主稿改判**：试点→**观察**，触发条件=第 3 节点加入或实测冷拉瓶颈 |
| D8 profiling | 部分改判：先 Go pprof/PGO，确需平台再 Parca 短 PoC | Parca PoC（同主稿） | **收敛**：不常驻；先原生 pprof，需要持续平台时 Parca 短 PoC（Apache-2.0） |
| D9 Kruise（低权重） | 仅 ImagePullJob | P-3 引进 | **收敛**：ImagePullJob 先用（大促镜像预热、零工作负载迁移），CloneSet 单服务试点降为可选 |

### E.2 主稿（captain）认账清单——两成员复核 §B 后追加命中

1. §10 与「五件事#1」仍写「Pigsty pgBackRest 异地化」→ 应统改 **CNPG Barman Cloud Plugin + 备份异地化**（Pigsty 已关机，A2）。
2. §2 与跨层联动图以「Pigsty 打包」论证 pgvector/pg_search 可行性 → pgvector 论据换「CNPG standard 镜像内置」；pg_search 零成本前提消失，激进路线降权。
3. 缓存基线仍写 Dragonfly → 实为 `redis.redis.svc`（TLS），dragonfly 属待清理残留（A4）。
4. §9 Spegel 试点 → 观察（D7）。
5. §12 Flipt v2 主选 → 降条件项（D3）。
6. §8 VL 直切 → 有界双写 ≤72h（D2）。
7. §4 ESO 立即引进 → 次序化（D1）。

### E.3 对两成员稿的裁决命中

- codex 稿：「⏸ 凭据」被改判为「▶ 带前置子任务」；「双写无时间盒」补时间盒与验收清单；「KEDA 等 NATS」推翻。保留其最有价值贡献：Velero≠DB 一致性恢复（Barman 分工）、R1/R3 具体形态、2 节点现实纪律。
- claude2 稿：Chaos Mesh 自我改判成立；截断章节立场重建被接受（D4 的 CNPG-pgvector 重建为本轮最有价值新证据）；其稿 L8 基线（Dragonfly/ES）与 P-3「Meili 替 ES」同样按 A3/A4 修正为收尾迁移项。

### E.4 第 2 轮候选议题

1. mirrord 替 Okteto（第 1 轮无人对抗，主稿孤证）。
2. CNPG standard 镜像 pgvector 实证 + Barman Cloud Plugin 备份异地化落地形态。
3. D2 的 72h 双写验收清单细化（PII 反例集、查询等价抽查样本）。
4. 三稿基线段的统一订正建议（回填 TECH-RADAR/STACK 的措辞）。
