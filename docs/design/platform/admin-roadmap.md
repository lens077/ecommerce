# 管理员角色能力取舍与路线图

> 2026-08-12 基于 [merchant/store-settings.md](../merchant/store-settings.md)（Shopline
> 调研）**反推**平台侧能力，并对照市面 B2B2C 平台 admin 标配（淘系/Shopee 平台侧、
> Mirakl、CRMEB/mall4j 等开源商城）给出差距与优先级。admin 是**角色不是微服务**，
> 治理能力横跨 merchant/order/product 等服务，故归 platform 目录；权限模型本体见
> [rbac.md](rbac.md)，商家侧对偶路线见 [merchant/roadmap.md](../merchant/roadmap.md)。
> 写作当日现状：admin 前端仅路由骨架（`index/users/merchants/products/orders/
> categories/reports/settings`，未接后端）；RBAC 里 admin 是不可分的原子角色；网关
> 已按 RPC 粒度放行商家审批/激活与 `inventory.v1.*` 给 admin。此后实况以 `TODO.md` 为准。

## 一、参考方式：从 Shopline 调研反推平台侧

Shopline 是独立站 SaaS，后台"管理员"= 商家自己（店长），没有平台管理员的直接样本；
扮演"平台"的是 SHOPLINE 官方。因此参考方式是反推——调研里每一处「SHOPLINE 替商家
做了什么 / 管着商家什么」，就是本项目 admin 该长出来的能力：

| 调研出处（store-settings.md） | Shopline 平台侧在做的事 | 对 admin 的启发 |
|---|---|---|
| 试用横幅、§13 套餐 | 商家生命周期：试用→付费→到期锁定 | 商家生命周期状态机（申请→审核→激活→冻结/清退），比 merchant 服务单一 `status` 字段完整 |
| §8 管理员和权限 | 店长/员工/合作伙伴 + 强制 2FA + 页面水印 | **唯一可原样照搬给 admin 的章节**：平台运营子账号、角色、2FA、水印防泄 |
| §16 操作日志 | 商家操作全量审计（IP/设备/菜单/事件，3 个月） | 双份审计：平台运营操作审计 + 可调阅商家操作日志（仲裁取证） |
| §14 账单、免佣额度、佣金规则 | 平台向商家计费：账单类型/状态/明细/导出 | 佣金规则配置 + 结算单管理侧（商家 roadmap P1 结算的另一半） |
| §10 通知（试用仅默认模板） | 平台统一管模板，按商家分层放开自定义 | 通知模板由 admin 统一管理，商家只有使用权 |
| §1 商品种类枚举、§15 政策模板库 | 平台定义行业分类、提供政策范本 | 类目/行业字典管理 + 平台政策模板库（商家「从模板创建」的源头） |
| §7 风险账号图形验证 | 平台级风控介入登录/注册 | 风控位置在平台层，不在商家层 |

## 二、现状 vs 市面 B2B2C 标配的差距

| 竞品标配模块 | 本项目现状 |
|---|---|
| 商家管理（入驻审核/资质/分层/处罚冻结） | 骨架有 `merchants` 路由；后端审批链路带全表 UPDATE 缺陷与 panic 桩——规划覆盖、实现为零 |
| 类目/属性/品牌库管理 | 骨架有 `categories`，TODO 规划有，未实现 |
| 商品治理（审核/违规下架） | 骨架有 `products`，但规划里只有「管理」没有「审核/下架」治理动作 |
| 订单查询与售后仲裁 | 骨架有 `orders`，TODO 规划有「订单仲裁」，未实现 |
| 资金侧（佣金规则/结算审核/对账） | 规划和骨架都没有——TODO「结算服务」只写了商家视角 |
| 内容治理（评价/举报） | 无评价体系，暂不适用 |
| 平台营销（活动/券） | 无营销服务，完全没有 |
| 风控（黑名单/异常行为） | 无；behavior 采了行为数据但没有风控消费方 |
| 平台运营子账号与审计 | admin 是原子角色，无子账号、无审计 |
| 数据大盘 | GMV/客单价口径已在 Grafana，但运营侧（admin 后台）看不到 |

**结构性差异**：现有规划的 admin 是「管理」（用户/商家/商品/订单四个名词的 CRUD），
竞品 admin 的核心是「治理」（审核、仲裁、处罚、风控四个动词）——**工作流缺位是最大差距**。

## 三、必须要补（不补则 B2B2C 闭环断裂）

1. **商家入驻审核闭环**：修 `ApproveApplication` 全表 UPDATE 与 `Reject`/`Activate`
   panic 桩，admin 前端接上审核列表/通过/驳回/激活——商家 roadmap P0 的对偶面，
   商家进不来后面全是空谈
2. **类目管理**：merchant 上架依赖类目树，admin 必须先能维护（骨架路由已在）
3. **商家状态管控**：冻结/恢复。注意与商家自愿的「休息」分开建模——休息是商家
   意愿，冻结是平台强制，状态来源不同不可共用一个字段
4. **订单仲裁最小面**：商家拒退时买家的上诉出口——已给商家定退货规则（merchant
   roadmap P0），无仲裁则规则只保护商家
5. **商品违规下架**：单个强制动作即可，不必先建完整审核流
6. **审计日志**：与商家 P0 审计同一张表同一套采集，admin 多一个全局查询视角；
   无取证数据的仲裁没有公信力
7. **RPC 授权收敛**：TODO 已列「其余服务仍整段放行待细化」——admin 权限过宽本身是安全债

## 四、强烈推荐（第二梯队，与商家 P1 同步）

1. **佣金规则 + 结算单审核**：商家 P1 做结算时，规则配置与审核必须在 admin 侧同步
   长出来，否则结算服务没有输入
2. **商品审核工作流**：从「违规下架」升级为可配置的先发后审/先审后发
3. **通知模板管理**：通知中心（商家 P1）落地时模板 CRUD 放 admin
4. **运营大盘进 admin**：把 Grafana 已有的 GMV/订单/客单价口径用 ECharts 搬进
   `reports` 路由（merchant 端 ECharts 按需 chunk 方案可复用）
5. **平台配置页**：政策模板库、协议文本、商品种类字典的维护入口（骨架 `settings` 路由）
6. **风控基线**：黑名单 + 登录异常标记，消费 behavior 已有数据
7. **平台运营子账号**：照搬 §8 店长/员工模型到平台侧，配 2FA；casdoor 有现成能力，
   工作量在 Casbin 策略分层（与 rbac.md 商家子账号是同一套机制的两次实例化）

## 五、明确不做

保证金体系、发票管理、广告位竞价、多级代理分销、商家等级付费权益——要么依赖资金
合规能力，要么是规模化之后的问题，练手项目投入产出比过低。

## 六、与商家路线图的对偶关系

| merchant/roadmap.md | 本文对偶项 |
|---|---|
| P0-1 修入驻缺陷 | 必补 1（同一件事的前后端两面） |
| P0-2 店铺资料/营业状态 | 必补 3（休息 vs 冻结分开建模） |
| P0-6 退货规则 | 必补 4（仲裁出口） |
| P0-8 审计简版 | 必补 6（同表同采集，双视角） |
| P1-1 结算与账单 | 推荐 1（规则与审核在 admin 侧） |
| P1-3 通知模板中心 | 推荐 3（模板管理权在 admin） |
| P1-2 员工与权限 | 推荐 7（同一套 Casbin 分层机制） |

排期原则：admin 的「必补」与商家 P0 同批做（互为依赖），「强烈推荐」与商家 P1 对齐。
