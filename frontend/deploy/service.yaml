apiVersion: v1
kind: Service
metadata:
  name: ecommerce-frontend-service
spec:
  externalTrafficPolicy: Local
  selector:
    app: ecommerce-frontend
  ports:
    - protocol: TCP
      port: 30080
      targetPort: 80
  type: LoadBalancer

