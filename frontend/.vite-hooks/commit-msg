# 提交信息校验。规则在仓库根的 commitlint.config.mjs。
#
# 为什么钩子在 frontend/ 下却管着整个仓库：core.hooksPath 是仓库级设置，
# 由 frontend/package.json 的 prepare(vp config) 指到 frontend/.vite-hooks/_，
# 所以后端 Go 的提交同样会走这里。别把它挪回仓库根的 .husky —— vp config
# 会主动接管任何 .husky 开头的 hooksPath，两套钩子抢同一个 git 配置，
# 抢输的那套就静默失效（2025-11 到 2026-08 的九个月就是这么没的）。
#
# 用 pnpm exec 而不是裸 commitlint：_/h 只把 frontend/node_modules/.bin
# 放进 PATH，而 commitlint 装在仓库根。钩子的 cwd 是仓库根，pnpm 从那儿
# 向上找到根 package.json，正好用上根的 node_modules/.bin。
# 不要加 --no —— pnpm 11 已经不认这个 exec 参数，会直接报
# 「Command "--no" not found」，等于把校验变成永远失败。
#
# 用 "$1"（git 传进来的消息文件路径）而不是写死 .git/COMMIT_EDITMSG——
# git commit --amend、rebase reword、以及 worktree 里的路径都不是那个值。
pnpm exec commitlint --edit "$1"
