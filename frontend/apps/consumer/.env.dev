# BFF 会话轨（control-tower ADR-0002）：dev 经 vite proxy 走同源，
# 否则 SameSite=Lax 的会话 cookie 不会被带上（前端与网关跨站）。
# 代理目标见 vite.config.ts 的 gatewayTarget（可用 GATEWAY_PROXY_TARGET 覆盖）。
VITE_GATEWAY_URL=/api
VITE_BFF_BASE_URL=
