下单:

1. 防止弱网/超时/重试,重复发送同一个请求到后端创建多个相同的订单
为什么会重复?
1. 人: 客户手抖点击多次, 网络慢, 客户以为没有成功, 又点一次
2. 网络: 弱网下前端/网关重试
3. 后端: 建单成功, 但响应超时, 客户端重发

解决方案:
1. 用户进入结算页时生成一个唯一的id(uuid)作为requestId, 用户在当前页面重新刷新时也不影响
2. 后端使用Redis的SET NX(单线程串行处理, 先进入的那条能设置成功)来保证原子性, 当多个请求发送时,只有先进入的请求成功创建,后续的请求自动拒绝返回"该订单已经创建"类似字样
