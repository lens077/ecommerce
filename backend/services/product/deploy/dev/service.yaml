apiVersion: v1
kind: Service
metadata:
  name: ecommerce-product-service
  namespace: ecommerce
spec:
  selector:
    app: ecommerce-product
  ports:
    - protocol: TCP
      port: 30003
      targetPort: 30003
  # 业务入口统一走共享 Gateway，不再占用 LB IP、L2 Lease 与 NodePort。
  type: ClusterIP
