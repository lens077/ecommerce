apiVersion: v1
kind: Service
metadata:
  name: ecommerce-address-service
  namespace: ecommerce
spec:
  selector:
    app: ecommerce-address
  ports:
    - protocol: TCP
      port: 30009
      targetPort: 30009
  # 业务入口统一走共享 Gateway，不再占用 LB IP、L2 Lease 与 NodePort。
  type: ClusterIP
