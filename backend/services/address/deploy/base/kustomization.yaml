---
{
  # 环境无关的部分。base 即 pre 基线(镜像由 CI 回写到这里);prod 在 overlays/prod/ 打补丁。
  # pre 直接 apply -k deploy/base;prod 用 kubectl apply -k deploy/overlays/prod。
  apiVersion: "kustomize.config.k8s.io/v1beta1",
  kind: "Kustomization",
  resources: [
    "deployment.yaml",
    "service.yaml",
  ],
}
