apiVersion: v1
kind: Service
metadata:
  name: ecommerce-user-service
  namespace: ecommerce
spec:
  selector:
    app: ecommerce-user
  ports:
    - protocol: TCP
      port: 30001
      targetPort: 30001
  # 业务入口统一走共享 Gateway，不再占用 LB IP、L2 Lease 与 NodePort。
  type: ClusterIP
