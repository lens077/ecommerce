apiVersion: v1
kind: Service
metadata:
  name: ecommerce-search-service
spec:
  selector:
    app: ecommerce-search
  ports:
    - protocol: TCP
      port: 30002
      targetPort: 30002
  type: LoadBalancer
