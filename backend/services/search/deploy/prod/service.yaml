apiVersion: v1
kind: Service
metadata:
  name: ecommerce-search-service
  namespace: ecommerce
spec:
  selector:
    app: ecommerce-search
  ports:
    - protocol: TCP
      port: 30002
      targetPort: 30002
  # 业务入口统一走共享 Gateway，不再占用 LB IP、L2 Lease 与 NodePort。
  type: ClusterIP
