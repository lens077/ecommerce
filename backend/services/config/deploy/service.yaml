apiVersion: v1
kind: Service
metadata:
  name: ecommerce-cart-service
spec:
  selector:
    app: ecommerce-cart
  ports:
    - protocol: TCP
      port: 30006
      targetPort: 30006
  type: LoadBalancer
