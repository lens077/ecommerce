apiVersion: v1
kind: Service
metadata:
  name: ecommerce-payment-service
spec:
  externalTrafficPolicy: Local
  selector:
    app: ecommerce-payment
  ports:
    - protocol: TCP
      port: 30008
      targetPort: 30008
  type: LoadBalancer
