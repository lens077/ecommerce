apiVersion: v1
kind: Service
metadata:
  name: ecommerce-payment-service
  namespace: ecommerce
spec:
  selector:
    app: ecommerce-payment
  ports:
    - protocol: TCP
      port: 30008
      targetPort: 30008
  # 业务入口统一走共享 Gateway，不再占用 LB IP、L2 Lease 与 NodePort。
  type: ClusterIP
