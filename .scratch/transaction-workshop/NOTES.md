# 教学备注

- 用户偏好：前端直接落实最佳实践；库存 → 后端购物车采用教学方式，目标是独立实现并排查问题。
- 尚未收到练习答案，不创建声称「已掌握」的 learning record。首次回答体现理解或暴露误区后，再按 teach 格式建立 learning-records/0001-*.md。
- 下次先问：同一个 ctx 中为何仍可能出现事务外写入？隔一次会话后再问，不先展示答案。
- 再交叉练习：库存扣减失败与购物车零行更新，分别应该观察哪些业务结果？
- 前端选择状态本轮只统一本地持久化；后端快照仍有覆盖权，不承诺服务端保存、多标签页同步或 SSR。
- 两节课的 JavaScript 只在浏览器本地运行，不连接数据库、不上传作答、不记录遥测。
- 课程文件是教学材料。实现状态查项目 TODO.md；不要把课程中的目标 interface 复制成第二份设计规范。
